## Service configuration
[log]
level = "info"
format = "text"

[server]
addr = "0.0.0.0:8080"
# Docker service name for inter-container communication
public_addr = "memoh-server:8080"

## Admin
[admin]
username = "admin"
password = "admin123"
email = "admin@memoh.local"

## Auth configuration
[auth]
jwt_secret = "YZq8kXrW5dFpNt9mLxQvHbRjKsMnOePw"
jwt_expires_in = "168h"

## Docker configuration
[containerd]
socket_path = "/run/containerd/containerd.sock"
namespace = "default"

[mcp]
image = "docker.io/library/memoh-mcp:latest"
snapshotter = "overlayfs"
data_root = "/opt/memoh/data"
data_mount = "/data"

## Postgres configuration
[postgres]
host = "postgres"
port = 5432
user = "memoh"
password = "memoh123"
database = "memoh"
sslmode = "disable"

## Qdrant configuration
[qdrant]
base_url = "http://qdrant:6334"
api_key = ""
collection = "memory"
timeout_seconds = 10

## Agent Gateway
[agent_gateway]
host = "0.0.0.0"
port = 8081
# Docker service name for inter-container communication (Server → Agent)
public_host = "memoh-agent"

## Web
[web]
host = "127.0.0.1"
port = 8082
